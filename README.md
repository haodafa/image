# Image

A repository for hosting images used in blogs and articles.

## Usage

Images are stored in the `images/` directory and can be referenced via GitHub raw URLs:

```
https://raw.githubusercontent.com/haodafa/image/main/images/<filename>
```
